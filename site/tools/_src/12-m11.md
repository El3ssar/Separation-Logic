# CHAPTER SOURCE — id=m11  num=M11  index=12
phase: Phase 3 · Advanced separation logic
title: The magic wand
blurb: Separating implication, and why it is the right adjoint of ∗ rather than a curiosity.

Blocks below are given in order. Each begins with `@@ <type> [n]`.

@@ h3 [0]
The idea

@@ code [1]
```
def wand (P Q : Assertion) : Assertion :=
  fun σ h => ∀ h', Heap.disjoint h h' → P σ h' → Q σ (Heap.union h h')
infixr:54 " -∗ " => wand
```

@@ quote [2]
I hold a resource such that, <b>if you give me a separate heap satisfying <code>P</code></b>, the combination satisfies <code>Q</code>.

@@ svg [3]
```

<svg viewBox="0 0 560 148" role="img" aria-label="The magic wand">
  <g class="dg">
    <rect x="14" y="34" width="130" height="38" rx="7" class="dg-box"/>
    <text x="79" y="58" text-anchor="middle" class="dg-t">h : P −∗ Q</text>
    <text x="160" y="58" class="dg-t">+</text>
    <rect x="184" y="34" width="110" height="38" rx="7" class="dg-box a"/>
    <text x="239" y="58" text-anchor="middle" class="dg-t">h' : P</text>
    <path class="dg-arr" d="M310 53 L 356 53"/>
    <rect x="372" y="34" width="168" height="38" rx="7" class="dg-box b"/>
    <text x="456" y="58" text-anchor="middle" class="dg-t">h ∪ h' : Q</text>
    <text x="14" y="104" class="dg-note">A wand is a resource-consuming promise: hand it a separate heap satisfying P</text>
    <text x="14" y="124" class="dg-note">and the combination satisfies Q. It is the right adjoint of ∗, not of ∧.</text>
  </g>
</svg>
```

@@ p [4]
The analogy with ordinary implication is exact, and so is the difference. Ordinary implication satisfies the currying adjunction

@@ txt [5]
```
  P ∧ Q → R      iff      P → (Q → R)
```

@@ p [6]
and the wand is defined so that the same holds with <code>∗</code> in place of <code>∧</code>:

@@ txt [7]
```
  P ∗ Q ⊢ R      iff      P ⊢ Q -∗ R
```

@@ note [8]
kind: key
title: The one thing to remember
<code>-∗</code> is the <b>right adjoint</b> of <code>∗</code>. That single sentence is the whole milestone. Assertions with <code>(∗, emp, -∗)</code> form a closed symmetric monoidal category — technically, a <i>BI algebra</i> — and separation logic is the internal logic of that structure. Once you know this, the laws below are not things to memorise; they are the standard consequences of an adjunction.

@@ p [9]
The practical reading: a wand is a <b>promise that consumes resources</b>. Ordinary implication <code>Q → R</code> can be used any number of times because assumptions are free. A wand can be used once, because using it consumes the heap you fed it. That is what makes it the right tool for “I have given away part of my data structure and here is how to put it back”.

@@ note [10]
kind: info
title: Why anyone cares
Where you meet wands in practice: <i>partial ownership</i>. If you hand out one node of a linked list to a subroutine, what remains is not a list — it is “a list with a hole”, which is exactly <code>node p x n -∗ listRep xs p</code>. Iris makes heavy use of this pattern under the name <i>magic wand as a continuation</i>.

@@ sec [11]
Exercises

@@ ex [12]
id: m11-1   name: wand_intro   hard: False
why: Currying. Half of the adjunction.
GOAL:
```
theorem wand_intro {P Q R : Assertion} (h : P ∗ Q ⊢ R) : P ⊢ Q -∗ R
```
hint: Given a heap <code>hh</code> satisfying <code>P</code> and a disjoint <code>h'</code> satisfying <code>Q</code>, feed the pair to <code>h</code> as a star with cut <code>⟨hh, h'⟩</code> and equation <code>rfl</code>.
SOLUTION:
```
theorem wand_intro {P Q R : Assertion} (h : P ∗ Q ⊢ R) : P ⊢ Q -∗ R := by
  intro σ hh hp h' hd hq
  exact h σ (Heap.union hh h') ⟨hh, h', hd, rfl, hp, hq⟩
```
expl: The heap equation is <code>rfl</code> because the goal’s heap <i>is</i> <code>Heap.union hh h'</code> — the wand’s definition puts it there. This is the cleanest possible instance of “choose the cut and everything follows”.

@@ ex [13]
id: m11-2   name: wand_elim   hard: False
why: Uncurrying, i.e. <i>modus ponens for resources</i>. Note that the wand and its argument must be in disjoint heaps — you cannot apply a wand to memory you have already spent.
GOAL:
```
theorem wand_elim (P Q : Assertion) : (P -∗ Q) ∗ P ⊢ Q
```
hint: Destructure the star and apply the wand to the second piece.
SOLUTION:
```
theorem wand_elim (P Q : Assertion) : (P -∗ Q) ∗ P ⊢ Q := by
  intro σ h ⟨h₁, h₂, hd, hu, hw, hp⟩
  rw [hu]
  exact hw h₂ hd hp
```
expl: <code>rw [hu]</code> turns the goal’s heap into <code>Heap.union h₁ h₂</code>, which is precisely the shape the wand produces. One line of content.

@@ ex [14]
id: m11-3   name: star_wand_adjunction   hard: False
why: The adjunction itself. Prove the ⟸ direction <i>from</i> the two previous lemmas rather than from scratch — that is the whole point of having them.
GOAL:
```
theorem star_wand_adjunction (P Q R : Assertion) :
    (P ∗ Q ⊢ R) ↔ (P ⊢ Q -∗ R)
```
hint: ⟹ is <code>wand_intro</code>. For ⟸: from <code>P ⊢ Q -∗ R</code>, monotonicity gives <code>P ∗ Q ⊢ (Q -∗ R) ∗ Q</code>, and then <code>wand_elim</code> finishes.
SOLUTION:
```
theorem star_wand_adjunction (P Q R : Assertion) : (P ∗ Q ⊢ R) ↔ (P ⊢ Q -∗ R) := by
  constructor
  · exact wand_intro
  · intro h
    refine entails_trans (star_mono_left Q h) ?_
    exact wand_elim Q R
```
expl: The backward direction is two lemma applications composed with <code>entails_trans</code>. In categorical language you have just verified that <code>(− ∗ Q)</code> is left adjoint to <code>(Q -∗ −)</code>, with <code>wand_elim</code> as the counit.

@@ ex [15]
id: m11-4   name: wand_mono   hard: False
why: Contravariant on the left, covariant on the right — exactly like a function type, and for exactly the same reason.
GOAL:
```
theorem wand_mono {P P' Q Q' : Assertion} (hp : P' ⊢ P) (hq : Q ⊢ Q') :
    (P -∗ Q) ⊢ (P' -∗ Q')
```
hint: Compose: turn the given <code>P'</code>-heap into a <code>P</code>-heap with <code>hp</code>, apply the wand, then push the result along <code>hq</code>.
SOLUTION:
```
theorem wand_mono {P P' Q Q' : Assertion} (hp : P' ⊢ P) (hq : Q ⊢ Q') :
    (P -∗ Q) ⊢ (P' -∗ Q') := by
  intro σ h hw h' hd hp'
  exact hq σ _ (hw h' hd (hp σ h' hp'))
```
expl: If this variance surprises you, look at the definition: <code>P</code> occurs to the left of an arrow, <code>Q</code> to the right. Everything else follows.

@@ dod [16]
You understand <code>∗</code> and <code>-∗</code> as an adjoint pair, not as two unrelated symbols, and you can prove the standard laws from the adjunction rather than from the model.
