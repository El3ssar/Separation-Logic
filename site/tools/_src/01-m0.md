# CHAPTER SOURCE — id=m0  num=M0  index=1
phase: Phase 1 · Semantic foundations
title: The Lean you actually need
blurb: Five techniques recur in every proof in this course. Get them into your fingers now.

Blocks below are given in order. Each begins with `@@ <type> [n]`.

@@ h3 [0]
The idea

@@ p [1]
You do not need much Lean for this course, but you need a handful of things <i>fluently</i>, because they appear in essentially every proof. Here they are, with the mathematician’s translation.

@@ h4 [2]
1. Propositions are types; proofs are terms

@@ p [3]
“<code>P → Q</code>” is the type of functions from proofs of <code>P</code> to proofs of <code>Q</code>. So <i>modus ponens is function application</i>. Nothing more is going on.

@@ code [4]
```
theorem implication_example (P Q : Prop) :
    P → (P → Q) → Q :=
  fun hp hpq => hpq hp
```

@@ h4 [5]
2. ∀ is a dependent function type; ∃ is a pair

@@ p [6]
To prove <code>∀ x, P x</code>, produce a function. To prove <code>∃ x, P x</code>, produce the pair (witness, proof) — written <code>⟨w, pf⟩</code>. To <i>use</i> an existential, take the pair apart with <code>obtain ⟨x, hx⟩ := h</code>.

@@ code [7]
```
theorem exists_example : ∃ n : Nat, n = 3 := ⟨3, rfl⟩
```

@@ note [8]
kind: info
title: The single most useful piece of syntax
<code>⟨…⟩</code> is Lean’s <i>anonymous constructor</i>. It nests and flattens automatically: for <code>∃ a b, P ∧ Q ∧ R</code> you may write <code>⟨a, b, hp, hq, hr⟩</code>. Separation-logic goals are deeply nested existentials-of-conjunctions, so this notation is the difference between a two-line proof and a twenty-line one.

@@ h4 [9]
3. Functions are equal when they agree pointwise

@@ p [10]
Heaps <i>are</i> functions, so every heap equation is proved by <code>funext</code> followed by case analysis on the location.

@@ code [11]
```
theorem function_extensionality {f g : Nat → Nat}
    (h : ∀ x, f x = g x) : f = g := funext h
```

@@ h4 [12]
4. Case analysis on <code>Option</code> and on decidable equalities

@@ p [13]
<code>cases hl : h l with | none => … | some v => …</code> both splits the case <i>and</i> records the equation <code>hl</code>, which you will need for rewriting. For <code>if x = l</code> style definitions, <code>by_cases hx : x = l</code> is the workhorse, and the two lemmas <code>if_pos</code> / <code>if_neg</code> do the rewriting.

@@ h4 [14]
5. A small, honest tactic vocabulary

@@ p [15]
<code>intro</code>, <code>exact</code>, <code>apply</code>, <code>refine</code>, <code>obtain</code> / <code>rcases</code>, <code>cases</code>, <code>by_cases</code>, <code>constructor</code>, <code>funext</code>, <code>rw</code>, <code>simp</code>, <code>subst</code>, <code>show</code>. That is the whole list. Resist the urge to reach for heavy automation early: in this subject the proofs <i>are</i> the content, and a proof that <code>simp</code> closed teaches you nothing about heaps.

@@ note [16]
kind: key
title: The one thing to remember
<code>show</code> is underrated. When a goal displays as <code>fact φ σ h</code> and you want to see <code>σ x = 3</code>, write <code>show σ x = 3</code>. It changes nothing logically (the two are definitionally equal) but it makes the goal readable, and it makes <code>simp</code> able to fire.

@@ sec [17]
Exercises · function update

@@ p [18]
Everything in this course is built on one primitive: pointwise update of a function. Define it once:

@@ code [19]
```
def update (f : Nat → Nat) (x value : Nat) : Nat → Nat :=
  fun y => if y = x then value else f y
```

@@ ex [20]
id: m0-1   name: update_same   hard: False
why: Reading back what you just wrote. Trivial mathematically — the point is to see how <code>if</code> unfolds.
GOAL:
```
theorem update_same (f : Nat → Nat) (x value : Nat) :
    update f x value x = value
```
hint: <code>simp [update]</code> will unfold the definition and discharge <code>if x = x</code>. If you want to see the mechanics, try <code>unfold update</code> then <code>rw [if_pos rfl]</code>.
SOLUTION:
```
theorem update_same (f : Nat → Nat) (x value : Nat) :
    update f x value x = value := by
  simp [update]
```
expl: <code>simp [update]</code> unfolds <code>update</code> to <code>if x = x then value else f x</code> and then uses the simp lemma <code>if_pos</code> with the reflexivity proof to reduce it to <code>value</code>. The goal becomes <code>value = value</code>, closed by <code>rfl</code>.

@@ ex [21]
id: m0-2   name: update_other   hard: False
why: Updating one key does not disturb any other. This is the “locality” of an update, in miniature — and it is exactly the shape of every heap lemma to come.
GOAL:
```
theorem update_other (f : Nat → Nat) (x y value : Nat) (hne : y ≠ x) :
    update f x value y = f y
```
hint: Feed the disequality to <code>simp</code>: <code>simp [update, hne]</code>. <code>simp</code> uses <code>hne</code> to fire <code>if_neg</code>.
SOLUTION:
```
theorem update_other (f : Nat → Nat) (x y value : Nat) (hne : y ≠ x) :
    update f x value y = f y := by
  simp [update, hne]
```
expl: Passing <code>hne</code> in the simp set lets <code>simp</code> rewrite <code>if y = x</code> to the else-branch. Note the direction of the hypothesis: <code>y ≠ x</code>, matching the order in the <code>if</code>. If you state it as <code>x ≠ y</code> you must insert <code>Ne.symm</code>.

@@ ex [22]
id: m0-3   name: update_shadow   hard: False
why: The first genuine <i>function equality</i>. Two updates to the same key: the later one wins and the earlier one is invisible.
GOAL:
```
theorem update_shadow (f : Nat → Nat) (x a b : Nat) :
    update (update f x a) x b = update f x b
```
hint: Start with <code>funext y</code>, then <code>by_cases h : y = x</code>. Both branches are one <code>simp</code>.
SOLUTION:
```
theorem update_shadow (f : Nat → Nat) (x a b : Nat) :
    update (update f x a) x b = update f x b := by
  funext y
  by_cases h : y = x <;> simp [update, h]
```
expl: <code>funext y</code> reduces equality of functions to equality at an arbitrary point <code>y</code>. Then <code>by_cases h : y = x</code> splits into the two branches, and <code>simp [update, h]</code> uses <code>h</code> (either <code>y = x</code> or <code>¬ y = x</code>) to collapse every <code>if</code>. The <code>&lt;;&gt;</code> combinator applies the same tactic to both goals.

@@ ex [23]
id: m0-4   name: update_comm   hard: False
why: Independent updates commute. This is the baby version of “disjoint heap updates commute”, which is what makes the frame rule true.
GOAL:
```
theorem update_comm (f : Nat → Nat) (x y a b : Nat) (hne : x ≠ y) :
    update (update f x a) y b = update (update f y b) x a
```
hint: <code>funext z</code>, <code>unfold update</code>, then three cases: <code>z = x</code> (so <code>z ≠ y</code>), <code>z = y</code>, and neither. Drive each branch with explicit <code>rw [if_pos …]</code> / <code>rw [if_neg …]</code> so you can see what is happening.
SOLUTION:
```
theorem update_comm (f : Nat → Nat) (x y a b : Nat) (hne : x ≠ y) :
    update (update f x a) y b = update (update f y b) x a := by
  funext z
  unfold update
  by_cases hzx : z = x
  · have hzy : z ≠ y := by rw [hzx]; exact hne
    rw [if_neg hzy, if_pos hzx, if_pos hzx]
  · by_cases hzy : z = y
    · rw [if_pos hzy, if_neg hzx, if_pos hzy]
    · rw [if_neg hzy, if_neg hzx, if_neg hzx, if_neg hzy]
```
expl: After <code>unfold update</code> the goal is a nest of <code>if</code>s on both sides. The three cases correspond to the three regions of <code>Nat</code>: at <code>x</code> only the <code>a</code>-update is visible, at <code>y</code> only the <code>b</code>-update, elsewhere neither. Note how <code>hne</code> is used exactly once, to derive <code>z ≠ y</code> from <code>z = x</code> — that is where non-aliasing enters. Doing this by hand with <code>if_pos</code>/<code>if_neg</code> rather than <code>simp</code> is worth it once: it is the same argument you will make about heaps a hundred times.

@@ dod [24]
You can prove equalities between functions by <code>funext</code> plus case analysis, and you can read a nested-<code>if</code> goal without flinching.
