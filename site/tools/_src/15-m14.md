# CHAPTER SOURCE — id=m14  num=M14  index=15
phase: Phase 3 · Advanced separation logic
title: Optional — allocation
blurb: Adding malloc without destroying the frame rule.

Blocks below are given in order. Each begins with `@@ <type> [n]`.

@@ h3 [0]
The problem

@@ p [1]
Every heap in this course has been created by assumption; nothing ever allocates. Adding allocation is where a naïve design breaks the frame rule, so it is worth doing carefully.

@@ p [2]
Suppose you add “allocate exactly location <code>l</code>”. Then

@@ txt [3]
```
  { emp }  alloc-at l  { l ↦ 0 }

  frame with  l ↦ 7 :

  { emp ∗ l ↦ 7 }  alloc-at l  { l ↦ 0 ∗ l ↦ 7 }     ← postcondition is FALSE
```

@@ p [4]
The postcondition is unsatisfiable, so the framed triple is vacuously… no: the <i>precondition</i> is satisfiable and the command runs, so the triple is genuinely false. The command is not local: its behaviour depends on memory it does not own.

@@ note [5]
kind: key
title: The one thing to remember
Locality is a property you can lose. The frame rule is only available to languages whose commands cannot observe or collide with memory outside their footprint. Allocation is the standard place where this is tested.

@@ h3 [6]
Option A · nondeterministic fresh allocation

@@ code [7]
```
| alloc : Var → Atom → Cmd

-- semantics: choose ANY unallocated location, store the value there,
-- and put the location in the destination variable.

theorem hoare_alloc :
    Hoare emp (.alloc x e)
      (aExists fun l => pure (fun σ => σ x = l) ∗ (l ↦ e.eval σ))
```

@@ p [8]
The existential over <code>l</code> is not cosmetic: it is <b>required</b> for locality. Framing changes which locations are already taken, hence which one the allocator picks — so no specification that names a particular location can be local.

@@ p [9]
Consequences to be aware of: <code>Exec</code> stops being deterministic, so <code>exec_deterministic</code> and everything derived from it (including <code>partial_of_total</code>) must be revisited, and the interpreter needs an allocation strategy fixed as a parameter.

@@ h3 [10]
Option B · finite heaps

@@ code [11]
```
abbrev Heap := List (Loc × Val)   -- with a no-duplicate-keys invariant
```

@@ p [12]
With finite heaps you can <i>prove</i> that a fresh location exists (take one greater than the maximum key), so allocation becomes deterministic and executable. The price is that every M1–M2 lemma has to be redone: lookup, insert, delete, disjointness, and — the painful one — heap extensionality, which is no longer <code>funext</code> but a lemma about lists modulo permutation.

@@ note [13]
kind: info
title: Which to pick
Both options are genuinely worth doing, and they teach different things. Option A teaches you what locality really demands. Option B teaches you how much of your development was secretly relying on the convenience of functional heaps. If you do only one, do A: it is a hundred lines and it changes how you read the frame rule.

@@ sec [14]
Exercises (open)

@@ ex [15]
id: m14-1   name: add nondeterministic allocation   hard: False
why: Extend <code>Cmd</code> and <code>Exec</code>; note where determinism breaks.
GOAL:
```

```
hint: None
SOLUTION:
```

```
expl: None

@@ ex [16]
id: m14-2   name: small-footprint alloc rule   hard: False
why: Prove the triple above from the semantics.
GOAL:
```

```
hint: None
SOLUTION:
```

```
expl: None

@@ ex [17]
id: m14-3   name: locality of alloc under framing   hard: False
why: Prove <code>HeapLocal (.alloc x e)</code> — this is where the existential earns its keep.
GOAL:
```

```
hint: None
SOLUTION:
```

```
expl: None

@@ ex [18]
id: m14-4   name: alloc, initialise, free   hard: False
why: Verify a program that allocates a cell, writes to it, reads it back and frees it, ending in <code>emp</code>.
GOAL:
```

```
hint: None
SOLUTION:
```

```
expl: None
