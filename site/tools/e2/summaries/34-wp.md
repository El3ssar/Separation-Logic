# Unit 31 · `wp` · Weakest preconditions
`site/content/34-wp.js` · 58 blocks (51 non-`ex`), **7 exercises**, 88 KB / 132 KB rendered.
8 `trace` (1 top-level), 12 `code` (7 `illustration`, 1 `sketch`), 4 `state`, 2 `cmp`, 1 each `defn`
`note key` `note info` `ol` `txt` `detail` `dod`. **Lean fragment untouched.** No ledger row, no waiver.
*Reviewed and revised — see "What review changed" at the bottom. **No Lean was edited at review**;
all 19 goal displays and all 9 quoted compiler errors were re-derived from Lean and diffed against
the page (0 unmatched), the 7 illustrations were recompiled as one file through local Lean **and**
through the reader's Lean, and every falsifiable `variants` claim was compiled.*

## One job
Define `wp`, show `Hoare P c Q` and `P ⊢ wp c Q` are *the same proposition*, and prove the equations
that turn a definition into a calculus.

## The hook I left (verbatim, final block; §D word for word)
> There is a limit to what has been computed, and it is not about `wp`. Every assertion so far
> describes a <i>bounded</i> number of cells. A linked list does not, and no finite combination of
> `↦` and `∗` can describe one.

Opens on `33-swap`'s hook without echoing its last clause: "The function that computes **them** has
been inside the definition of a Hoare triple since Unit 22."

## Introduced
**Syntax (my rows):** `Iff.rfl`; `rfl` at `Prop`.
**First done by tactic:** a `⊣⊢` goal split by `constructor` and proved as two `intro` blocks (Units
12–16 always wrote `⟨_, _⟩`); `rename_i old` inside a `wp` proof.
**Names now usable:** `wp`, `hoare_iff_entails_wp`, `wp_skip`, `wp_assign`, `wp_seq`, `wp_mono`,
`wp_sound`, `wp_weakest`, `heap_eq_singleton`, `wp_free_emp`, `wp_write`, `wp_frame`.
**Illustration-only — seven anonymous `example`s, so no names and no collisions:** the three
`Iff.rfl`/`rfl` unfoldings · the three-command fold · the four Unit-22 rules re-derived · `wp c aTrue`
as `free`'s safety condition · `l ↦ 0` not the weakest precondition of a write · the reverse of
`wp_frame`, refuted · `wp_frame` in tactic mode with `show`.
**Concepts:** predicate transformer · **angelic** taken, **demonic** rejected (`cmp`), `PartialHoare`
named as the demonic definition already in the course · `wp c Q` a *greatest* element, not *the*
greatest, `⊢` being a preorder — uniqueness only up to `⊣⊢` · **forwards = invert a derivation,
backwards = build one** · transformers compose backwards · **forwards versus backwards as two
methods** (second `cmp`, added at review — §D asks for it): Unit 29 looks one command ahead and
carries what it needs as a frame with a `Preserves` obligation per command, the fold looks at the
whole tail and carries nothing; a fold with no choice points is a program and a search is not ·
*weakest* as two theorems · `wp c aTrue` is safety · the small-footprint precondition with the unread
value quantified away, and **why** (a rule's parameter is what its premise reads and its conclusion
discards).

## Exercises (page = fragment order)
`m12-1` **wp_skip / wp_assign** [C 2] — first `⊣⊢` by `constructor`; `subst` earned, not given.
`m12-2` **wp_seq** [C 3] — the fold from the end; the daggered middle state.
`m12-3` **wp_mono** [D 2] — the congruence that makes the fold typecheck.
`x63` **wp_sound and wp_weakest** [**G** 1] — *state* soundness and optimality; both proofs identity.
`m12-4` **wp_free_emp** [C 4, hard] — `wp` computed; `heap_eq_singleton` handed over as setup.
`m12-5` **wp_write** [C 4, hard] — the same existential; two definitional restatements.
`x64` **wp_frame** [D 1] — one term; the punchline.

## Warnings — every one compiled or grepped
1. **ERRATA §28 does NOT bite `Iff.rfl`.** `theorem hoare_iff_entails_wp … := Iff.rfl` unfolds two
   plain `def`s and **the reader's Lean accepts it** — re-run at review: prelude-34 `--incl` plus all
   seven page illustrations through `wasm-check.cjs` is *clean*. So does `example … = … := rfl`.
   §28 forbids a **`theorem` whose `:= rfl` proves an `Eq`** needing a `def` unfolded. Do not
   generalise it.
2. **Downstream (word boundaries, `35-`…`41-`): `wp` **3**, all in `38-partial`'s `wp_ite`; every
   theorem here **ZERO**. **Author of `38-partial`:** `wp_ite` is the only consumer — and it is
   proved by exactly the two moves this unit teaches, so cite the pattern rather than re-deriving it.
3. **`m12-1`'s `sol` holds two theorems, so `lint.mjs` counts the walk against both** —
   `indexOf(':= by')` finds the first. 12 lines, floor 6, it has exactly 6. One more line breaks it.
4. **`funext` occurs in no fragment after this one**, so `m12-5`'s "last pointwise heap argument" holds.
   (Unit 25's `payoff` promised "the next proof that does is five units away" — that is Unit 30's
   `swap_heap`; this unit is the one after it, and the last.)
5. **`heap_eq_singleton` is SETUP** — marker below it (ERRATA §21); §D objective 5 is met on the main
   line, with the unit's only top-level `trace`.
6. **`rw [hl]` fails after inverting `Exec.write`** (*pattern `{ store := σ, heap := h }.heap x`*),
   hence `m12-5`'s two `have` restatements — `28-local-heap` warning 4 again. `rename_i` is needed in
   `m12-5` (the witness must be *written*) and not in `m12-4` (the `_` is determined).
7. **`hoare_frame wp_sound hlocal hpres` does not compile** — `wp_sound`'s `c`, `Q` are explicit.
8. **`HeapLocal c` is provable for an arbitrary `c`** — eight lines, one per constructor, citing the
   eight locality lemmas of Units 24–26 (compiled at review). So dropping `hlocal` from `wp_frame`
   leaves a statement that is true **and provable**, just not in one line. The earlier claim "the
   proof is gone" was wrong; `x64`'s `variants` now says what is actually lost.
9. **Compiled at review, not shipped as text:** dropping `hpres` from `wp_frame` is *false*
   (`c = x := 5`, `Q = emp`, `R = fact (fun σ => σ x = 0)`) · `heap_eq_singleton` without `hrest` is
   false (`Heap.write (Heap.singleton 0 1) 1 2`) · both `m12-2` `variants` counterexamples
   (`wp c₁ Q` alone, and the two transformers in the wrong order) · `x63`'s uniqueness statement as
   `⟨wp_weakest h, huniv (wp c Q) (wp_sound c Q)⟩` · **`m12-5`'s `copyCell` claim** — the refutation
   needs `src ≠ dst` to build the witness state, and the `variants` field now says so.
10. **"Hardest theorem in the course" is taken.** `23-induction` tells the reader the two hardest
    proofs left are `heapLocal_loop` (Unit 26) and the invariant rule (Unit 36). Do not award that
    title to `hoare_frame` — this page now says it is the theorem that *cost* the most: three units
    of locality machinery before Unit 27 could state it.

## Deviations
1. **Seven `ex`, not six** — §F pairs `m12-4`+`m12-5`; the tooling forbids two ids on one `ex`. Both
   hard and consecutive: §F's ceiling, with a difficulty-1 on each side.
2. 8 `trace` against 2; 51 non-`ex` blocks against ~32 (ERRATA §18 — summit unit).
3. The `detail` carries only the `PartialHoare` failure; "unfolding by hand" is on the main line as
   three compiled `example`s, so closing the fold leaves no hole.
4. **§D says `x63` is [D]; §F row 121 and §F.1 say [G].** I followed §F — ERRATA §22 placeholder
   `goal`, statements at rung 3, two other correct statements in `variants`.
5. The four Unit-22 rules re-derived are mine, not §D's; all four compile.
6. **Two `cmp`, not §D's one.** §D's size line asks for a forwards-versus-backwards `cmp` and §D's
   "New mathematics" asks for both that *and* angelic-versus-demonic. One block cannot be both: the
   angelic one sits where the definition is made, the forwards/backwards one closes the calculus
   section.

## Provenance
All 19 goal displays are `check.sh 34 [--incl]` `trace_state` output, byte for byte, from eight
snippets; four are trimmed and each says so in its own step. **Regenerated from scratch at review and
diffed against the page: 0 unmatched.** Nine quoted errors, each from breaking a real proof, all
re-derived at review — the `PartialHoare` `Iff.rfl` mismatch · `introN` on an unsplit `⊣⊢` ·
`exact hq` before `cases hex` · the two `Unknown identifier` from an unnamed `cases` · `wp_mono`'s
entailment applied at the wrong state · the `Heap.empty` final state rejected by `Exec.free` · the
`rw` pattern failure in `m12-5` · `hoare_frame wp_sound`. The metavariable indices in the quoted
`Exec.free` mismatch depend on how much of the proof surrounds it and will not match a reader's
verbatim; everything else is stable. Green: `node --check`; `lint.mjs` **0/0**; `ledger.mjs` **0/0**,
frags clean, sweep 3 (pre-existing); `render-check.js` **0**; `verify.sh 34`; `wasm-check.cjs` on
prelude-34 `--incl` + all 7 illustrations **clean**; banned grep **0**.

## What review changed
No ids, no names, no exercise `goal`/`sol`, no Lean, no `trace`, no `state`, no hint rung. Three
blocks added, eleven fields rewritten.

**The contract hole.** §D's size line specifies a `cmp` of **forwards versus backwards**, and §D's
"New mathematics" wants "forwards versus backwards reasoning, and why every practical verification
tool is built on `wp`". Neither was on the page: every use of the two words was about the two
directions of a `⊣⊢` proof, and `orient.payoff`'s claim that "practical verification tools are built
on this definition, and the reason is on this page" was unsupported. Added at the end of the calculus
section (a `p`, a `cmp`, a `p`): Unit 29 chooses each intermediate assertion with **one command of
lookahead**, so a fact established early and needed late travels as a frame and costs a `Preserves`
obligation per command — `preserves_load_fact` is exactly one of those; the fold looks at the whole
tail, chooses nothing and carries nothing; a fold with no choice points can be run by a program and a
search over frames cannot. The `dod` gained the matching clause.

**Facts that were wrong.**
- **"large enough to be correct … and large enough to be optimal"**, in the sentence that says the
  two halves of *weakest* "pull in opposite directions". Soundness needs `wp c Q` **small** enough
  that nothing unsafe is inside it. Rewritten.
- **"the resulting definition is three lines"** — it is two. That sentence also carried the page's
  only `we`; neighbouring units have none.
- **The `defn` card's "every — here, the only — outcome satisfies `Q`"**. That is the demonic gloss
  patched with determinism, two paragraphs after the `cmp` chose the angelic reading and one section
  before the `note` that insists nothing here needs determinism. Now says what the definition says:
  there is a run, it finishes, and where it finishes satisfies `Q`.
- **`x64` `variants`: "the proof is gone"** when `hlocal` is dropped. Warning 8 — it is provable in
  eight lines. Now says what is really lost: `HeapLocal` for a *variable* command is an induction,
  not the lookup Unit 29 promised.
- **"the hardest theorem in the course"** for `hoare_frame`. Warning 10.
- **"That is a general fact about small-footprint rules"** — asserted from two instances, with the
  reasoning deleted. Replaced by the reasoning: a rule's parameter is the value its premise reads out
  of the heap, and its conclusion either uses it or discards it; `free` erases the cell and `write`
  overwrites it, so neither run depends on which value it was. The overclaim is withdrawn in the same
  breath ("two commands do not make a theorem, and stating one would mean quantifying over rules").
- **`m12-1`'s `why`: "Unit 22 … asked you to take on trust that this was the right notion".** Unit 22
  did not ask for trust; it defended semantic `subst` on economy (no substitution lemma, no
  induction). Now says that, and that economy is not a reason to believe a definition is *right*.
- **`m12-3`'s `why`: "the reason `wp` deserves the name transformer".** Monotonicity is not what makes
  a predicate transformer one. Now: what makes `wp c` a map of the entailment preorder *to itself*.

**Prose.** `m12-2`'s `variants` ran "the right-hand side is inhabited by nothing at all, because
`wp ([0] := 1) emp` is:" — an ellipsis the reader has to reconstruct; rewritten with the reason
stated (a write leaves the cell it wrote allocated, so it never lands in `emp`). "The practical
consequence arrives immediately and keeps arriving" → "The consequence is practical and it is
immediate". "Which heaps was it empty *from*?" → "Which heaps could it have started from?". "That
reversal is worth one more sentence", followed by three sentences → "worth a closing remark".

## Weakest part, honestly
`x63`, `x64` are difficulty 1 and `m12-3` is two lines, so the unit is measured by `m12-4`/`m12-5` —
heap arithmetic of a kind done since Unit 06. **`hoare_iff_entails_wp`, the real content, is not an
exercise**: §D gives it no id, so the reader reads it and only `x64` makes them use it. `x63`'s
placeholder `goal` means the editor checks nothing they write. The angelic/demonic `cmp` is
half-refuted: the demonic `Iff.rfl` fails on the page, but no block exhibits a command where the two
readings disagree, since Unit 35 owns that. And the new forwards/backwards `cmp` is read, not done:
nothing asks the reader to *run* the fold on a program, because the fold needs a `wp` equation per
command and the heap commands only have theirs against fixed postconditions — which is the honest
limit of the calculus this unit ships.
